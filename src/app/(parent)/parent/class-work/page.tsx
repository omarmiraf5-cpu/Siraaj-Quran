"use client";

import { ParentClassWork } from "@/components/class-work-ui";

/** A parent's view of their children's Islamic Studies and Arabic work: answers, marks and comments. */
export default function ParentClassWorkPage() {
  return <ParentClassWork />;
}
